const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    extend: {
      colors: {
        normal: '#08081b',
      },
      lineHeight: {
        headline: '1.1',
      }
    },
  },
  variants: {},
  plugins: [],
}
